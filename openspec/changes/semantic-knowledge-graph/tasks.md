# Tasks: Semantic Knowledge Graph

- [ ] Update `pyharness/schemas.py` with new node and edge enum types.
- [ ] Refactor `pyharness/phases/analyze.py` to implement the 3-pass analysis (Discovery, Mapping, Consolidation).
- [ ] Update frontend `KnowledgeGraph` component to render new node and edge types visually.
- [ ] Add tests for the new analysis phases in `tests/test_analyze.py`.
- [x] Verify frontend visualization works with a sample semantic knowledge graph.